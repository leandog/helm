<Window x:Class="AppSwitcher.HiddenWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:av="clr-namespace:Avalon.Windows.Controls;assembly=AvalonLibrary"
        Title="HiddenWindow" Height="0" Width="0" Visibility="Hidden" WindowState="Minimized" WindowStyle="None">
    <av:NotifyIcon Icon="switch.ico" Text="AppSwitcher" IconVisibility="Visible">
        <FrameworkElement.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Test Metro" Click="OnTestClicked"/>
                <MenuItem Header="Test IExplore" Click="OnTestClicked2"/>
                <MenuItem Header="Exit" Click="OnExitClicked"/>
            </ContextMenu>
        </FrameworkElement.ContextMenu>
    </av:NotifyIcon>
</Window>
